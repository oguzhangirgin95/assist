# PRD Workflow

## Goal
Create or validate a PRD with clear scope and measurable acceptance criteria.

## Steps
1. Ask mode: create | validate | edit.
2. If create: fill `template`.
3. If validate/edit: load existing PRD and run `checklist`.
4. Ensure acceptance criteria are testable.

## Output
Write to `default_output_file`.
