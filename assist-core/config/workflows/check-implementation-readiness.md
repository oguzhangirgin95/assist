# Check Implementation Readiness

## Goal
Find gaps before coding: missing ACs, unclear UX, missing NFRs, missing test plan.

## Steps
1. Load PRD, UX, Architecture, Epics/Stories if present.
2. Run `checklist`.
3. Output a report: PASS/CONCERNS/FAIL with required fixes.

## Output
Write to `default_output_file`.
