# Document Project

## Goal
Scan an existing codebase and create a structured documentation set.

## Steps
1. Ask for target code root (default: {project-root}).
2. Identify languages/frameworks/build tooling.
3. Produce: source tree overview, key modules, runtime architecture, testing, deployment.
4. Save docs under `output_folder`.
