# Sprint Planning

## Goal
Create or update `sprint-status.yaml` tracking stories and progress.

## Steps
1. If `sprint_status` missing, create from `template`.
2. Ask for which epics/stories to include.
3. Update statuses and owners.
